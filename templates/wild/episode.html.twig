{% extends 'base.html.twig' %}
{% block title %}{{ program.title }}-Saison {{ season.number }}-Episode {{ episode.number }}{% endblock %}

{% block body %}

    <h1><a href="/wild/program/{{ program.title }}">{{ program.title }}</a> / <a href="/wild/season/{{ season.id }}">Saison {{ season.number }}</a></h1>
        <div>
            <h2>Episode {{ episode.number }} : {{ episode.title }}</h2>
            <p>{{ episode.synopsis }}</p>
        </div>
    <a href="{{ path('app_index') }}">
        Retour à l'accueil
    </a>

{% endblock %}